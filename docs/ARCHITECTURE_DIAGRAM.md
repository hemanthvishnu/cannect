# 🌿 Cannect Architecture Diagram

> **Version:** 1.1.0 | **Protocol:** AT Protocol (Bluesky) | **Platform:** Expo + React Native

---

## 🏗️ High-Level System Architecture

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              CANNECT APP (Client)                               │
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                         PRESENTATION LAYER                               │   │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐        │   │
│  │  │   (tabs)/   │ │   (auth)/   │ │   /user/    │ │   /post/    │        │   │
│  │  │   feed      │ │   welcome   │ │   [handle]  │ │ [did]/[rkey]│        │   │
│  │  │   search    │ │   login     │ │   followers │ │             │        │   │
│  │  │   compose   │ │   register  │ │   following │ │             │        │   │
│  │  │   notifs    │ │             │ │             │ │             │        │   │
│  │  │   profile   │ │             │ │             │ │             │        │   │
│  │  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘        │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                      │                                          │
│                                      ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                          STATE LAYER                                    │   │
│  │  ┌───────────────────────┐    ┌───────────────────────────────────┐    │   │
│  │  │   Zustand Store       │    │    TanStack Query (React Query)   │    │   │
│  │  │   ─────────────       │    │    ────────────────────────────   │    │   │
│  │  │   • session           │    │    • Timeline / Feed              │    │   │
│  │  │   • profile           │    │    • Cannect Feed                 │    │   │
│  │  │   • isAuthenticated   │    │    • Author Feed                  │    │   │
│  │  │   • did / handle      │    │    • Profiles                     │    │   │
│  │  │                       │    │    • Notifications                │    │   │
│  │  │   auth-store-atp.ts   │    │    • Post Threads                 │    │   │
│  │  └───────────────────────┘    └───────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                      │                                          │
│                                      ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                          HOOKS LAYER                                    │   │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐         │   │
│  │  │  use-atp-auth   │  │  use-atp-feed   │  │ use-atp-profile │         │   │
│  │  │  ─────────────  │  │  ─────────────  │  │ ─────────────── │         │   │
│  │  │  useLogin()     │  │  useTimeline()  │  │  useProfile()   │         │   │
│  │  │  useLogout()    │  │  useCannectFeed │  │  useMyProfile() │         │   │
│  │  │  useCreateAcct  │  │  useCreatePost  │  │  useFollow()    │         │   │
│  │  │  useCurrentDid  │  │  useLikePost()  │  │  useUnfollow()  │         │   │
│  │  │                 │  │  useRepost()    │  │  useFollowers() │         │   │
│  │  │                 │  │  usePostThread  │  │  useFollowing() │         │   │
│  │  └─────────────────┘  └─────────────────┘  └─────────────────┘         │   │
│  │                                                                         │   │
│  │  ┌─────────────────┐  ┌─────────────────┐                              │   │
│  │  │ use-atp-notifs  │  │   use-pwa       │                              │   │
│  │  │ ─────────────── │  │   ───────       │                              │   │
│  │  │ useNotifications│  │   usePWA()      │                              │   │
│  │  │ useUnreadCount  │  │   setBadge()    │                              │   │
│  │  │ useMarkAsRead   │  │   isInstalled   │                              │   │
│  │  └─────────────────┘  └─────────────────┘                              │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                      │                                          │
│                                      ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                       ATPROTO AGENT LAYER                               │   │
│  │                                                                         │   │
│  │                    ┌─────────────────────────┐                          │   │
│  │                    │    lib/atproto/agent    │                          │   │
│  │                    │    ──────────────────   │                          │   │
│  │                    │  • BskyAgent singleton  │                          │   │
│  │                    │  • Session persistence  │                          │   │
│  │                    │  • RichText parsing     │                          │   │
│  │                    │  • API method wrappers  │                          │   │
│  │                    └───────────┬─────────────┘                          │   │
│  │                                │                                        │   │
│  │               ┌────────────────┴────────────────┐                       │   │
│  │               ▼                                 ▼                       │   │
│  │    ┌──────────────────┐              ┌──────────────────┐              │   │
│  │    │   SecureStore    │              │   AsyncStorage   │              │   │
│  │    │   (Native)       │              │   (Web/PWA)      │              │   │
│  │    │   Session JWT    │              │   Session JWT    │              │   │
│  │    └──────────────────┘              └──────────────────┘              │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                 │
└──────────────────────────────────────┬──────────────────────────────────────────┘
                                       │
                                       │ HTTPS
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           AT PROTOCOL NETWORK                                   │
│                                                                                 │
│   ┌───────────────────────────────────────────────────────────────────────┐    │
│   │                                                                       │    │
│   │    ┌────────────────────┐         ┌────────────────────────────┐     │    │
│   │    │                    │         │                            │     │    │
│   │    │  cannect.space     │◄───────►│  Bluesky Relay / AppView   │     │    │
│   │    │  (Our PDS)         │  Sync   │  api.bsky.app              │     │    │
│   │    │                    │         │                            │     │    │
│   │    │  • User accounts   │         │  • Global search           │     │    │
│   │    │  • Posts (records) │         │  • Feed aggregation        │     │    │
│   │    │  • Follows         │         │  • Notification routing    │     │    │
│   │    │  • Likes           │         │  • Content indexing        │     │    │
│   │    │  • Profile data    │         │                            │     │    │
│   │    │                    │         │                            │     │    │
│   │    └────────────────────┘         └────────────────────────────┘     │    │
│   │              │                                  │                     │    │
│   │              │                                  │                     │    │
│   │              ▼                                  ▼                     │    │
│   │    ┌────────────────────────────────────────────────────────────┐    │    │
│   │    │                   feed.cannect.space                       │    │    │
│   │    │                   (Custom Feed Generator)                  │    │    │
│   │    │                                                            │    │    │
│   │    │    Curates cannabis-related content from AT Protocol      │    │    │
│   │    │    network + all posts from cannect.space users           │    │    │
│   │    └────────────────────────────────────────────────────────────┘    │    │
│   │                                                                       │    │
│   └───────────────────────────────────────────────────────────────────────┘    │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 📱 Screen Navigation Flow

```
                              ┌─────────────┐
                              │   app/      │
                              │  _layout    │
                              │  (Root)     │
                              └──────┬──────┘
                                     │
           ┌─────────────────────────┼─────────────────────────┐
           │                         │                         │
           ▼                         ▼                         ▼
┌──────────────────┐      ┌──────────────────┐      ┌──────────────────┐
│    index.tsx     │      │     (auth)/      │      │     (tabs)/      │
│  ─────────────   │      │  ─────────────   │      │  ─────────────   │
│  Auth check      │─────►│  welcome.tsx     │      │  Bottom tabs     │
│  Redirect based  │      │  login.tsx       │─────►│  after login     │
│  on session      │      │  register.tsx    │      │                  │
└──────────────────┘      └──────────────────┘      └────────┬─────────┘
                                                              │
           ┌──────────────┬──────────────┬──────────────┬────┴────────┐
           │              │              │              │             │
           ▼              ▼              ▼              ▼             ▼
    ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐
    │   feed    │  │  search   │  │  compose  │  │  notifs   │  │  profile  │
    │   ─────   │  │  ──────   │  │  ───────  │  │  ──────   │  │  ───────  │
    │ Cannect + │  │ User &    │  │ Create    │  │ Likes     │  │ Own      │
    │ Following │  │ Post      │  │ new post  │  │ Reposts   │  │ profile  │
    │ feeds     │  │ search    │  │ w/ media  │  │ Follows   │  │ + tabs   │
    └─────┬─────┘  └─────┬─────┘  └───────────┘  └─────┬─────┘  └─────┬─────┘
          │              │                              │             │
          ▼              ▼                              ▼             ▼
   ┌─────────────────────────────────────────────────────────────────────┐
   │                        DETAIL SCREENS                               │
   │                                                                     │
   │   ┌──────────────────┐   ┌──────────────────┐   ┌───────────────┐  │
   │   │ /post/[did]/     │   │ /user/[handle]   │   │ /settings/    │  │
   │   │ [rkey].tsx       │   │                  │   │ edit-profile  │  │
   │   │ ───────────      │   │ ─────────────    │   │ ────────────  │  │
   │   │ Thread view      │   │ Other user's     │   │ Update name   │  │
   │   │ with replies     │   │ profile + tabs   │   │ bio, avatar   │  │
   │   │ ancestors        │   │ followers/ing    │   │ banner        │  │
   │   └──────────────────┘   └──────────────────┘   └───────────────┘  │
   │                                                                     │
   └─────────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         AUTHENTICATION FLOW                             │
└─────────────────────────────────────────────────────────────────────────┘

  ┌──────────┐                                              ┌──────────────┐
  │  App     │                                              │  cannect.space│
  │  Start   │                                              │  (PDS)        │
  └────┬─────┘                                              └───────┬──────┘
       │                                                            │
       │  1. Check stored session                                   │
       ▼                                                            │
  ┌──────────────────────┐                                          │
  │ SecureStore/         │                                          │
  │ AsyncStorage         │                                          │
  │ ────────────────     │                                          │
  │ Read SESSION_KEY     │                                          │
  └──────────┬───────────┘                                          │
             │                                                      │
      ┌──────┴──────┐                                               │
      │             │                                               │
      ▼             ▼                                               │
┌──────────┐  ┌──────────────┐                                      │
│ No       │  │ Has stored   │                                      │
│ session  │  │ session JWT  │                                      │
│          │  │              │                                      │
│ Show     │  │ 2. Resume    │                                      │
│ Welcome  │  │    session   │──────────────────────────────────────┤
│ screen   │  └──────────────┘                                      │
│          │         │                                              │
│          │         ▼                                              │
│          │  ┌────────────────┐     3. Validate JWT                │
│          │  │ bskyAgent.     │─────────────────────────────────►  │
│          │  │ resumeSession()│                                    │
│          │  └────────────────┘                                    │
│          │         │                                              │
│          │  ┌──────┴──────┐                                       │
│          │  ▼             ▼                                       │
│          │  Valid       Invalid                                   │
│          │  ────────    ───────                                   │
│          │  Go to       Clear                                     │
│          │  Feed        storage                                   │
│          │              → Welcome                                 │
└──────────┘                                                        │
     │                                                              │
     │  User clicks "Sign In"                                       │
     ▼                                                              │
┌──────────────────────┐                                            │
│  login.tsx           │                                            │
│  ────────────────    │                                            │
│  identifier (handle) │                                            │
│  password            │                                            │
│                      │  4. Login request                          │
│  useLogin().mutate() │───────────────────────────────────────────►│
└──────────────────────┘                                            │
     │                                                              │
     ◄──────────────────────────────────────────────────────────────┤
     │  5. Return session (accessJwt, refreshJwt, did, handle)      │
     ▼                                                              │
┌──────────────────────┐                                            │
│  Auth Store Update   │                                            │
│  ──────────────────  │                                            │
│  setSession(data)    │                                            │
│  isAuthenticated=true│                                            │
│  did = session.did   │                                            │
│  handle = session.   │                                            │
│          handle      │                                            │
└──────────────────────┘                                            │
     │                                                              │
     ▼                                                              │
┌──────────────────────┐                                            │
│  Navigate to         │                                            │
│  /(tabs)/feed        │                                            │
└──────────────────────┘                                            
```

---

## 📤 Create Post Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          CREATE POST FLOW                               │
└─────────────────────────────────────────────────────────────────────────┘

  ┌──────────────┐                                       ┌──────────────┐
  │  compose.tsx │                                       │ cannect.space │
  │  ────────────│                                       │ (PDS)         │
  └──────┬───────┘                                       └───────┬──────┘
         │                                                       │
         │  1. User types post                                   │
         ▼                                                       │
  ┌──────────────────┐                                           │
  │  State:          │                                           │
  │  • text          │                                           │
  │  • images[]      │                                           │
  │  • video         │                                           │
  │  • reply context │                                           │
  └──────┬───────────┘                                           │
         │                                                       │
         │  2. User taps "Post"                                  │
         ▼                                                       │
  ┌──────────────────────────────────────┐                       │
  │  useCreatePost().mutateAsync()       │                       │
  │  ────────────────────────────────    │                       │
  │                                      │                       │
  │  ┌────────────────────────────────┐  │                       │
  │  │  3. Upload media (if any)     │  │  uploadBlob()          │
  │  │     → Returns blob refs       │──┼──────────────────────► │
  │  └────────────────────────────────┘  │                       │
  │                                      │  ◄──── blob.ref       │
  │  ┌────────────────────────────────┐  │                       │
  │  │  4. Parse RichText            │  │                       │
  │  │     • Detect @mentions        │  │                       │
  │  │     • Detect URLs             │  │                       │
  │  │     • Detect #hashtags        │  │                       │
  │  │     → Create facets[]         │  │                       │
  │  └────────────────────────────────┘  │                       │
  │                                      │                       │
  │  ┌────────────────────────────────┐  │                       │
  │  │  5. Create post record        │  │  com.atproto.repo     │
  │  │     {                         │  │  .createRecord()       │
  │  │       text,                   │──┼──────────────────────► │
  │  │       facets,                 │  │                       │
  │  │       embed: { images[] },    │  │                       │
  │  │       reply?: { parent, root },│  │                       │
  │  │       createdAt               │  │                       │
  │  │     }                         │  │                       │
  │  └────────────────────────────────┘  │                       │
  │                                      │  ◄── { uri, cid }    │
  └──────────────────────────────────────┘                       │
         │                                                       │
         │  6. Mutation onSuccess                                │
         ▼                                                       │
  ┌──────────────────────────────────────┐                       │
  │  queryClient.invalidateQueries()     │                       │
  │  ──────────────────────────────────  │                       │
  │  • ['timeline']                      │                       │
  │  • ['cannectFeed']                   │                       │
  │  • ['authorFeed']                    │                       │
  │  • ['thread', rootUri] (if reply)    │                       │
  └──────────────────────────────────────┘                       │
         │                                                       │
         ▼                                                       │
  ┌──────────────────────────────────────┐                       │
  │  Navigate back to feed               │                       │
  │  Post appears immediately            │                       │
  └──────────────────────────────────────┘                       │
```

---

## 🗂️ Component Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        COMPONENT HIERARCHY                              │
└─────────────────────────────────────────────────────────────────────────┘

components/
├── ui/                      ← Reusable base components
│   ├── Avatar.tsx              • Profile avatar with fallback
│   ├── Button.tsx              • Styled button variants
│   ├── Loading.tsx             • Loading spinners
│   ├── Skeleton.tsx            • Content placeholder
│   ├── Tabs.tsx                • Tab navigation
│   ├── Toast.tsx               • Toast notifications
│   ├── VideoPlayer.tsx         • Video playback
│   ├── ZoomableImage.tsx       • Pinch-to-zoom images
│   └── MediaViewer.tsx         • Full-screen media modal
│
├── social/                  ← Social feature components
│   ├── ProfileHeader.tsx       • Profile banner, stats, follow btn
│   ├── PostMedia.tsx           • Image/video grid in posts
│   ├── PostCarousel.tsx        • Swipeable image gallery
│   ├── PostOptionsMenu.tsx     • ... menu (delete, share, etc)
│   ├── PostShareCard.tsx       • Share sheet card
│   ├── ReplyBar.tsx            • Reply input at bottom
│   ├── RepostMenu.tsx          • Repost/Quote post menu
│   ├── DiscoveryModal.tsx      • User suggestions modal
│   ├── EmptyFeedState.tsx      • Empty feed placeholder
│   ├── PostSkeleton.tsx        • Post loading placeholder
│   └── ThreadSkeleton.tsx      • Thread loading placeholder
│
├── Post/                    ← Post display components
│   └── PostCard.tsx            • Feed item card
│
├── Profile/                 ← Profile components
│   ├── ProfileRow.tsx          • User row in lists
│   └── MediaGridItem.tsx       • Grid item for media tab
│
├── Search/                  ← Search components
│   └── TrendingDiscovery.tsx   • Trending/suggested content
│
├── notifications/           ← Notification components
│   └── NotificationItem.tsx    • Single notification row
│
└── (Root Level)             ← App-wide components
    ├── ErrorBoundary.tsx       • Catch React errors
    ├── ErrorState.tsx          • Error display
    ├── OfflineBanner.tsx       • Offline indicator
    ├── PWAUpdater.tsx          • PWA update prompt
    ├── IOSInstallPrompt.tsx    • Add to Home Screen prompt
    └── WhatsNewToast.tsx       • Changelog toast
```

---

## 💾 Data Flow Summary

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          DATA FLOW                                      │
└─────────────────────────────────────────────────────────────────────────┘

                     ┌──────────────────┐
                     │   Screen/Page    │
                     │   (React)        │
                     └────────┬─────────┘
                              │
                              │ Uses hooks
                              ▼
                     ┌──────────────────┐
                     │   Custom Hooks   │
                     │ ────────────────  │
                     │ useTimeline()    │
                     │ useProfile()     │
                     │ useCreatePost()  │
                     └────────┬─────────┘
                              │
          ┌───────────────────┼───────────────────┐
          │                   │                   │
          ▼                   ▼                   ▼
   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
   │  useQuery   │    │useMutation  │    │useInfinite  │
   │  (read)     │    │  (write)    │    │   Query     │
   │             │    │             │    │  (scroll)   │
   └──────┬──────┘    └──────┬──────┘    └──────┬──────┘
          │                  │                  │
          └──────────────────┼──────────────────┘
                             │
                             ▼
                     ┌──────────────────┐
                     │  AT Protocol     │
                     │  Agent           │
                     │  ────────────    │
                     │  lib/atproto/    │
                     │  agent.ts        │
                     └────────┬─────────┘
                              │
                              │ HTTPS / XRPC
                              ▼
                     ┌──────────────────┐
                     │  PDS             │
                     │ cannect.space    │
                     │                  │
                     │  + AppView       │
                     │  api.bsky.app    │
                     └──────────────────┘
```

---

## 🔑 Key Files Reference

| Layer | File | Purpose |
|-------|------|---------|
| **Entry** | `app/_layout.tsx` | Root layout, providers, auth init |
| **Auth** | `lib/stores/auth-store-atp.ts` | Zustand auth state |
| **Agent** | `lib/atproto/agent.ts` | BskyAgent singleton, API calls |
| **Hooks** | `lib/hooks/use-atp-auth.ts` | Login, logout, register |
| **Hooks** | `lib/hooks/use-atp-feed.ts` | Feed, posts, interactions |
| **Hooks** | `lib/hooks/use-atp-profile.ts` | Profile CRUD, follow |
| **Cache** | `lib/query-client.ts` | TanStack Query config |
| **Theme** | `constants/theme.ts` | Colors, spacing |
| **PWA** | `public/sw.js` | Service worker |
| **PWA** | `public/manifest.json` | Web app manifest |

---

## 🌐 External Services

| Service | URL | Purpose |
|---------|-----|---------|
| **PDS (Own)** | `cannect.space` | User data, posts, auth |
| **AppView** | `api.bsky.app` | Search, global feeds |
| **Feed Gen** | `feed.cannect.space` | Cannect curated feed |
| **Hosting** | Vercel | PWA/Web deployment |

---

*Generated: December 2024 | Cannect v1.1.0*
